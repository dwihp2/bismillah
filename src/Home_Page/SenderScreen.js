import React from 'react';
import { View, Text, Stylesheet } from 'react-native';
import { styles } from '../styles';

const SenderScreen = (props) => (
    <View style = {styles.container}>
        <Text>SenderScreen</Text>
    </View>
)
export default SenderScreen;