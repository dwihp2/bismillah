import React from 'react';
import { View, Text, Stylesheet } from 'react-native';
import { styles } from '../styles';

const ReceiverScreen = (props) => (
    <View style = {styles.container}>
        <Text>ReceiverScreen</Text>
    </View>
)
export default ReceiverScreen;